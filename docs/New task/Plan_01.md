# Миграция fammy.pet → Intent-Based Architecture v3

## Контекст

Переход от монолитного Hero к масштабируемой архитектуре: Bayan (3 кнопки) + CategorySheet overlay + отдельные страницы инструментов + одноколоночный адаптивный дизайн (Bayan everywhere).

---

## Принятые решения

| Вопрос                    | Решение                                                                                  |
| ------------------------- | ---------------------------------------------------------------------------------------- |
| Умный поиск (NLP/Fuse.js) | ❌ Убрать полностью. Не нужен на данном этапе                                             |
| Выбор функции             | Bayan (3 кнопки) на странице → клик → CategorySheet → ToolSheet                          |
| SEO                       | Каждый инструмент — отдельная страница (`/food-safety-check`, `/nutrient-analysis`, ...) |
| Загрузка в Sheet          | Пошаговая (Wizard): шаг 1 → шаг 2 → шаг 3. Кешируется при первом клике                   |
| Десктоп                   | Single Column (Bayan everywhere), без сайдбара. Центрированный лейаут.                   |
| Нижние секции             | Оставить как есть (SupportCard, WaitlistCTA, MiniFounder)                                |
| 26 видов животных         | Показывать все, неподдерживаемые — бейдж «Скоро». Бэкенд расширяется через 1-2 недели    |
| Параллельность            | Git branch `landing-v3`, деплоится только `main`                                         |
| Откат после слияния       | `git revert <merge-commit>` — одна команда                                               |

---

## Текущая архитектура (что НЕ трогать)

| Файл                                 | Строк    | Роль                              |
| ------------------------------------ | -------- | --------------------------------- |
| app/page.tsx                         | 28       | Оркестратор production            |
| components/landing/hero.tsx          | 480      | F1/F2, species, promo, результаты |
| components/landing/feature-cards.tsx | 108      | 2 карточки F1/F2                  |
| components/landing/support-card.tsx  | 225      | Тарифы поддержки                  |
| components/landing/waitlist-cta.tsx  | 108      | Email waitlist                    |
| components/landing/mini-founder.tsx  | 267      | Lifetime membership               |
| `app/api/*`                          | 8 routes | Backend proxy — НЕ МЕНЯТЬ         |
| lib/backendApi.ts                    | 197      | API клиент (dog/cat/human)        |
| lib/i18n.ts                          | 630      | 4 языка — ТОЛЬКО APPEND           |
| `components/ui/`                     | 51 файл  | Shadcn Kit — переиспользовать     |

---

## Новая структура файлов (ветка `landing-v3`)

app/

├── page.tsx                    # ЗАМЕНЯЕТСЯ в конце (cutover)

├── draft/page.tsx              # Draft-оркестратор (dev only)

├── food-safety-check/page.tsx  # F2 отдельная страница

├── nutrient-analysis/page.tsx  # F1 отдельная страница

├── portion-calculator/page.tsx # F3 отдельная страница (скоро)

├── recipe-generator/page.tsx   # F4 (скоро)

├── bcs-tracker/page.tsx        # F5 (скоро)

└── nutrients-guide/page.tsx    # F6 (скоро)

components/

├── landing/                    # НЕ ТРОГАТЬ до cutover

└── landing-v3/

    ├── hero-v3.tsx             # Минимальный Hero: заголовок + слоган
    ├── category-bayan.tsx      # 3 кнопки категорий (Баян)
    ├── category-sheet.tsx      # Оверлей выбора инструментов (светлый фон)
    ├── tool-sheet.tsx          # Sheet-обёртка для инструментов
    
    ├── pet-selector.tsx        # Модал выбора вида (26 видов)
    
    ├── f2-tool/
    
    │   ├── step-species.tsx    # Шаг 1: выбор вида
    
    │   ├── step-search.tsx     # Шаг 2: поиск продукта
    
    │   └── step-result.tsx     # Шаг 3: результат
    
    ├── f1-tool/
    
    │   ├── step-ingredients.tsx
    
    │   ├── step-analyze.tsx
    
    │   └── step-result.tsx
    
    └── shared/
    
        └── coming-soon.tsx     # Заглушка для F3-F6

lib/

├── species-config.ts           # 26 видов, группы, backendTarget

└── functions-config.ts         # F1-F6, категории, доступность

---

## 🛡️ Правила качества (для агента-разработчика)

CAUTION

**Эти правила ОБЯЗАТЕЛЬНЫ к соблюдению на каждом шаге. Нарушение любого из них — стоп-сигнал.**

### Запреты

| Код    | Запрет                                                              | Почему                                                    |
| ------ | ------------------------------------------------------------------- | --------------------------------------------------------- |
| **H1** | ❌ Никаких `// TODO`, `// FIXME`, `// HACK` в итоговом коде          | Временные решения не попадают в прод                      |
| **H2** | ❌ Никаких `as any`, `@ts-ignore`, `@ts-expect-error`                | Каждый тип должен быть корректным                         |
| **H3** | ❌ Никаких хардкодов (цвета, размеры, строки) без токенов            | Всё через Tailwind токены или i18n                        |
| **H4** | ❌ Не копировать логику — выносить в общий модуль                    | Дублирование ведёт к рассинхрону                          |
| **H5** | ❌ Не модифицировать файлы из `components/landing/` и `app/page.tsx` | Это production, его трогаем ТОЛЬКО при cutover            |
| **H6** | ❌ Не делать более 3 попыток исправить одну ошибку                   | Если не получается — остановиться и сообщить пользователю |

### Обязательства

| Код    | Обязательство                                                                         | Когда                 |
| ------ | ------------------------------------------------------------------------------------- | --------------------- |
| **Q1** | `npx tsc --noEmit` без ошибок                                                         | После КАЖДОГО коммита |
| **Q2** | Визуальная проверка на 360px и 1440px                                                 | После каждой фазы     |
| **Q3** | Каждый компонент принимает `className?` для кастомизации                              | При создании          |
| **Q4** | Все пользовательские строки — через `t()` (i18n)                                      | При создании          |
| **Q5** | Все новые i18n ключи — во всех 4 языках (en, ua, es, fr)                              | При добавлении        |
| **Q6** | `git diff --stat` перед каждым коммитом — проверить что не затронуты файлы production | Перед коммитом        |

### 🛑 Протокол коммуникации (STOP/CONTINUE)

| Команда      | Правило                                                                                                                                          |
|:------------ |:------------------------------------------------------------------------------------------------------------------------------------------------ |
| **STOP**     | Міттєва зупинка будь-яких автономних дій, інструментів або коммітів.                                                                             |
| **Питання**  | Якщо ви поставили питання в стані STOP, я маю надати вичерпну відповідь і зупинитися.                                                            |
| **CONTINUE** | Продовження роботи можливе ЛИШЕ після вашої явної команди («продовжуй», «continue», «ок», «виконуй»). Жодних паралельних дій під час очікування. |
| **Заборона** | Будь-яка зміна коду або стану репозиторію після команди стоп без підтвердження — це критична помилка.                                            |

---

## 🔄 Защита от зацикливания

WARNING

**Правило трёх попыток (H6)**: Если ошибка не исправлена за 3 итерации — СТОП. Агент обязан:

1. Зафиксировать текущее состояние (`git stash`)
2. Описать проблему текстом (что ожидалось, что получилось, что пробовал)
3. Передать контекст пользователю
4. **НЕ продолжать** пока не получит инструкцию

**Признаки зацикливания:**

* Одна и та же ошибка TypeScript после 2 правок
* Правка в файле А ломает файл Б, правка Б ломает А
* Компонент рендерится не так, как ожидалось, после 3 попыток стилизации

---

## 🧠 Защита от потери контекста

**Проблема:** Агент может потерять контекст между сессиями — забыть что было сделано, на каком шаге остановились, какие решения приняты.

**Решение — 4 уровня защиты:**

| Уровень                    | Механизм            | Что содержит                           | Частота обновления |
| -------------------------- | ------------------- | -------------------------------------- | ------------------ |
| 1. **task.md**             | Чеклист фаз         | Какая фаза `[x]`/`[/]`                 | После каждой фазы  |
| 2. **Git commits**         | Commit messages     | Что сделано                            | Каждый коммит      |
| 3. **CONTEXT.md**          | Файл в репозитории  | Состояние, решения, следующий шаг      | После каждой фазы  |
| 4. **MCP context-manager** | PostgreSQL + Qdrant | Ошибки, исправления, контрольные точки | При каждом событии |

### Уровень 4: MCP context-manager (подробно)

MCP context-manager — это база данных, которая хранит записи в двух хранилищах:

* **PostgreSQL** — поиск по ключевым словам, датам, агенту, сессии (`cm_query`)
* **Qdrant** — семантический векторный поиск по смыслу (`cm_search`)

**Когда агент ОБЯЗАН записать в MCP:**

| Событие                          | Инструмент MCP | Топики                       | Пример                                                                                                       |
| -------------------------------- | -------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Завершение фазы                  | `cm_save_im`   | `v3, phaseN, checkpoint`     | «Phase 3 завершена. PetSelector работает, 26 видов, toast для неподдерживаемых»                              |
| Ошибка TypeScript                | `cm_save_im`   | `v3, error, typescript`      | «Ошибка: Type 'string' is not assignable to... в pet-selector.tsx:42. Исправлено добавлением типа SpeciesId» |
| Исправление бага                 | `cm_save_im`   | `v3, bugfix, componentName`  | «Sheet не закрывался после выбора функции. Причина: onSelect не вызывал onClose»                             |
| Контрольная точка (до одобрения) | `cm_save_br`   | —                            | Краткое: «CP6: draft собран, F1/F2 работают, десктоп 2-колонки ОК, ждёт одобрения»                           |
| Принятие архитектурного решения  | `cm_save_im`   | `v3, decision, architecture` | «Решено: Sheet 85% высоты на мобильных вместо 50%»                                                           |
| Смена сессии (начало работы)     | `cm_save_br`   | —                            | «Начало сессии. Читаю CONTEXT.md, текущая фаза: Phase 4»                                                     |

**Как агент восстанавливает контекст в начале сессии:**

1. Прочитать CONTEXT.md                    ← текущая фаза, следующий шаг

2. cm_search q="v3 checkpoint" n=3         ← последние контрольные точки (семантический поиск)

3. cm_query agent="antigravity" date=today ← всё что было сегодня (SQL-поиск)

4. Прочитать task.md                       ← чеклист выполненных фаз

IMPORTANT

Агент ОБЯЗАН выполнить шаги 1-4 в начале КАЖДОЙ новой сессии, прежде чем делать любые изменения в коде.

**`CONTEXT.md`** (создаётся в Phase 0, обновляется после каждой фазы):

markdown

# Landing V3 — Контекст разработки

## Текущая фаза: [Phase N]

## Что сделано: [список завершённых фаз]

## Следующий шаг: [конкретное действие]

## Принятые решения: [ссылка на implementation_plan.md]

## Известные проблемы: [если есть]

## Последний MCP checkpoint: [дата и краткое содержание]

---

## Фазы реализации

### Phase 0: Подготовка ветки и контекста

**Цель:** Создать изолированное рабочее пространство

bash

git checkout -b landing-v3

git push -u origin landing-v3

Создать `CONTEXT.md` в корне проекта.

**Коммит:** `chore: create landing-v3 branch and CONTEXT.md`

**🔍 Контрольная точка 0:**

* [ ]  Ветка `landing-v3` создана и запушена
* [ ]  `CONTEXT.md` существует
* [ ]  `main` ветка не затронута

---

### Phase 1: Конфигурации и i18n

**Цель:** Бизнес-логика без UI

**Файлы:**

* `[NEW] lib/species-config.ts` — 26 видов, группы, `backendTarget`
* `[NEW] lib/functions-config.ts` — F1-F6, категории, доступность
* `[MODIFY] lib/i18n.ts` — APPEND ~40 новых ключей (категории, виды, UI)

**Правила:**

* `backendTarget: null` для видов без поддержки бэкенда (всё кроме dog/cat/human)
* `available: false` для функций F3-F6
* i18n: добавлять ключи В КОНЕЦ каждого языкового блока, не вставлять в середину

**Коммит:** `feat(v3): add species config, functions config, i18n keys`

**🔍 Контрольная точка 1:**

* [ ]  `npx tsc --noEmit` — без ошибок
* [ ]  `git diff --stat` — затронуты ТОЛЬКО `lib/` файлы
* [ ]  Все 4 языка содержат новые ключи
* [ ]  Ни один существующий ключ i18n не изменён

---

### Phase 2: Каркас draft + отдельные страницы

**Цель:** Создать маршруты и скелеты компонентов

**Файлы:**

* `[NEW] app/draft/page.tsx` — draft-лендинг
* `[NEW] app/food-safety-check/page.tsx` — F2 отдельная страница
* `[NEW] app/nutrient-analysis/page.tsx` — F1 отдельная страница
* `[NEW] app/portion-calculator/page.tsx` — F3 заглушка «Скоро»
* `[NEW] components/landing-v3/` — папка с placeholder-компонентами

Страницы F4-F6: создавать как заглушки с `ComingSoon` компонентом.

Каждая страница имеет свой `metadata` (title, description, openGraph) для SEO.

**Коммит:** `feat(v3): scaffold draft route, tool pages, component stubs`

**🔍 Контрольная точка 2:**

* [ ]  `http://localhost:3003/draft` — показывает скелет
* [ ]  `http://localhost:3003/food-safety-check` — показывает заголовок
* [ ]  `http://localhost:3003/` — production БЕЗ ИЗМЕНЕНИЙ
* [ ]  `npx tsc --noEmit` — без ошибок

---

### Phase 3: Pet Selector + Coming Soon

**Цель:** Переиспользуемые компоненты выбора вида и заглушки

**Файлы:**

* `[NEW] components/landing-v3/pet-selector.tsx`
* `[NEW] components/landing-v3/shared/coming-soon.tsx`

**PetSelector:**

* Shadcn `Sheet` (мобильный) / `Dialog` (десктоп)
* Top-4: Dog, Cat, Bird, Hamster — крупные кнопки с эмодзи
* «Інші види»: expandable группы (Гризуни, Рептилії, Екзотика)
* Виды с `backendTarget: null` → toast «Цей вид буде підтриманий пізніше»
* Пропс: `onSelect: (species: SpeciesEntry) => void`

**ComingSoon:** Компонент-заглушка для F3-F6 страниц. Показывает название функции + «Скоро доступно» + кнопка waitlist.

**Коммит:** `feat(v3): add PetSelector modal and ComingSoon component`

**🔍 Контрольная точка 3:**

* [ ]  PetSelector открывается, показывает 26 видов
* [ ]  Клик на неподдерживаемый вид — toast, не крашит
* [ ]  Все строки через `t()`, нет хардкодов
* [ ]  `npx tsc --noEmit` — без ошибок

---

### Phase 4: Tool Sheet + F2 Wizard

**Цель:** Sheet-обёртка и первый пошаговый инструмент

**Файлы:**

* `[NEW] components/landing-v3/tool-sheet.tsx` — Sheet-контейнер с навигацией шагов
* `[NEW] components/landing-v3/f2-tool/step-species.tsx`
* `[NEW] components/landing-v3/f2-tool/step-search.tsx`
* `[NEW] components/landing-v3/f2-tool/step-result.tsx`

**ToolSheet:**

* Обёртка: Sheet (мобильный, 85% высоты) / Dialog (десктоп)
* Внутри: `currentStep` state, кнопка «← Назад», заголовок шага
* Анимация перехода между шагами (slide, 200ms)
* Кнопка «↗ Відкрити на сторінці» → `router.push('/food-safety-check')`
* Кешируется при первом открытии

**F2 Wizard (3 шага):**

1. `StepSpecies` — выбор вида (4 кнопки + expandable). Логика из `PetSelector`
2. `StepSearch` — поиск продукта. Переиспользует `FoodAutocomplete` из `components/ui/`
3. `StepResult` — результат. Логика извлечена из текущего Hero (строки 54-102)

**Правило H4:** Логика API-вызова `checkFood()` НЕ дублируется — вызывается из `lib/backendApi.ts`.

**Коммит:** `feat(v3): add ToolSheet wrapper and F2 safety check wizard`

**🔍 Контрольная точка 4:**

* [ ]  Sheet открывается, шаги переключаются
* [ ]  F2 check работает end-to-end: вид → продукт → результат
* [ ]  Кнопка «← Назад» возвращает на предыдущий шаг, данные сохранены
* [ ]  «↗ Відкрити на сторінці» открывает `/food-safety-check`
* [ ]  На мобильном (360px) Sheet занимает 85% экрана
* [ ]  `npx tsc --noEmit` — без ошибок

---

### Phase 5: F1 Wizard + Category Accordion

**Цель:** Второй инструмент и навигация по категориям

**Файлы:**

* `[NEW] components/landing-v3/f1-tool/step-ingredients.tsx`
* `[NEW] components/landing-v3/f1-tool/step-analyze.tsx`
* `[NEW] components/landing-v3/f1-tool/step-result.tsx`
* `[NEW] components/landing-v3/category-bayan.tsx`
* `[NEW] components/landing-v3/category-sheet.tsx`
* `[MODIFY] app/draft/page.tsx` — подключить Bayan + CategorySheet

**F1 Wizard (3 шага):**

1. `StepIngredients` — добавление ингредиентов + вес. Логика из Hero (строки 104-148)
2. `StepAnalyze` — кнопка «Аналізувати» + загрузка
3. `StepResult` — таблица нутриентов + копирование. Логика из Hero (строки 183-212)

**CategoryBayan + CategorySheet:**

* 3 кнопки категорий (🛡️ Безпека, 🥗 Харчування, 💊 Здоров'я)
* Клик по кнопке відкриває `CategorySheet` (overlay)
* Усередині `CategorySheet` — список інструментів категорії
* Клик на доступний інструмент відкриває `ToolSheet` з відповідним визардом

**Коммит:** `feat(v3): add F1 nutrient wizard and category accordion`

**🔍 Контрольная точка 5:**

* [ ]  Accordion показывает 3 категории, функции внутри
* [ ]  Клик на F2 → Sheet → wizard работает
* [ ]  Клик на F1 → Sheet → wizard работает
* [ ]  Клик на F3-F6 → ComingSoon внутри Sheet
* [ ]  `npx tsc --noEmit` — без ошибок

---

**Цель:** Финальная сборка draft-лендинга (Single Column)

**Файлы:**

* `[NEW] components/landing-v3/hero-v3.tsx` — минимальный Hero
* `[NEW] components/landing-v3/promo-block.tsx` — перенос промокода
* `[MODIFY] app/draft/page.tsx` — финальная сборка
* `[MODIFY] app/food-safety-check/page.tsx` — полноценная F2 страница
* `[MODIFY] app/nutrient-analysis/page.tsx` — полноценная F1 страница

**Single Column Layout (Bayan Mode):**

* Никаких сайдбаров и `DesktopLayout`.
* Чистая центрованная структура: Nav → Слоган → Bayan → Promo → Support.
* На десктопе сохраняется аналогичная структура, но с максимальной шириной контейнера (max-w-2xl).

**Сборка draft/page.tsx:**

tsx

<Nav />

<main className="max-w-2xl mx-auto px-5">
  <section className="text-center py-16">
    <h1 className="text-4xl font-bold">Smart AI for Pet</h1>
    <p>Everything for safety and health</p>
    <CategoryBayan onSelect={handleCategoryClick} />
  </section>

<PromoBlock />
  <SupportCard />
  <WaitlistCTA />
  <MiniFounder />
</main>

<CategorySheet ... />
<ToolSheet ... />
<Footer />

**Коммит:** `feat(v3): assemble complete draft landing with desktop layout`

**🔍 Контрольная точка 6 (КРИТИЧЕСКАЯ):**

* [ ]  `/draft` — полностью функциональный лендинг
* [ ]  Десктоп (1440px): двухколоночный, сайдбар sticky
* [ ]  Мобильный (360px): одна колонка, всё читается
* [ ]  F2 wizard end-to-end: выбрал вид → нашёл продукт → получил результат
* [ ]  F1 wizard end-to-end: добавил ингредиенты → получил нутриенты
* [ ]  «↗ Відкрити на сторінці» работает для F1 и F2
* [ ]  Нижние секции (Support, Waitlist, Founder) отображаются корректно
* [ ]  Production `/` — БЕЗ ИЗМЕНЕНИЙ
* [ ]  `npx tsc --noEmit` — без ошибок
* [ ]  Все 4 языка работают

IMPORTANT

**СТОП.** После Phase 6 — обязательная демонстрация пользователю. НЕ переходить к Phase 7 без явного одобрения.

---

### Phase 7: Cutover (замена production)

**Цель:** Переключить production на новую архитектуру

CAUTION

Это единственная фаза, которая затрагивает production. Выполнять ТОЛЬКО после одобрения пользователя.

**Шаг 7a — Merge в main:**

bash

git checkout main

git merge landing-v3 --no-ff -m "feat: launch v3 landing architecture"

**Шаг 7b — Обновить `app/page.tsx`:** Заменить импорты с `landing/` на `landing-v3/`. Удалить `app/draft/`.

**Шаг 7c — Push:**

bash

git push origin main

**Коммит:** `feat: cutover to v3 landing architecture`

**Откат (если что-то пошло не так):**

bash

git revert HEAD    # отменяет merge-коммит

git push origin main

# Production мгновенно возвращается к старому лендингу

**🔍 Контрольная точка 7:**

* [ ]  `fammy.pet/` — новый лендинг работает
* [ ]  `fammy.pet/food-safety-check` — F2 работает
* [ ]  `fammy.pet/nutrient-analysis` — F1 работает
* [ ]  Все нижние секции на месте
* [ ]  Мобильный + десктоп проверены

---

### Phase 8: Очистка (через 1 неделю)

После подтверждения стабильности:

* Удалить `components/landing/` (старые компоненты)
* Переименовать `components/landing-v3/` → `components/landing/`
* Обновить все импорты
* Удалить неиспользуемые i18n ключи

**Коммит:** `chore: remove legacy landing components, rename v3 to landing`

---

## Сводная таблица фаз

| Фаза | Что делает                 | Трогает prod?  | Контрольная точка            |
| ---- | -------------------------- | -------------- | ---------------------------- |
| 0    | Ветка + CONTEXT.md         | ❌              | Ветка создана                |
| 1    | Конфиги + i18n             | ⚠️ append i18n | tsc + diff                   |
| 2    | Маршруты + скелеты         | ❌              | Все URL отвечают             |
| 3    | PetSelector + ComingSoon   | ❌              | 26 видов, toast              |
| 4    | ToolSheet + F2 wizard      | ❌              | E2E F2 check                 |
| 5    | F1 wizard + Accordion      | ❌              | E2E F1 + навигация           |
| 6    | Hero V3 + Desktop + сборка | ❌              | **СТОП → демо пользователю** |
| 7    | Cutover                    | ✅ ДА           | Production проверка          |
| 8    | Очистка                    | ✅ ДА           | Импорты, нет мёртвого кода   |
