# Протокол взаємодії та Правила якості (v3)

Цей документ є розширенням основних інструкцій агента для проекту fammy.pet.

## 🛡️ Правила якості (H1-H6, Q1-Q6)

### Заборони:
- **H1**: Жодних `// TODO`, `// FIXME`, `// HACK` в ітоговом коді.
- **H2**: Жодних `as any`, `@ts-ignore`, `@ts-expect-error`. Кожен тип має бути коректним.
- **H3**: Жодних хардкодів. Лише Tailwind токени та i18n.
- **H4**: Не копіювати логіку — виносити в спільні модулі.
- **H5**: **НЕ чіпати** `components/landing/` та `app/page.tsx` (це production).
- **H6**: Максимум 3 спроби на фікс однієї помилки (Захист від зациклення).

### Зобов'язання:
- **Q1**: `npx tsc --noEmit` без помилок після кожного комміту.
- **Q2**: Візуальна перевірка (360px та 1440px) після кожної фази.
- **Q3**: Кожен компонент приймає `className?`.
- **Q4**: Усі рядки через `t()` (i18n).
- **Q5**: Нові i18n ключі додаються в усі 4 мови (en, ua, es, fr).
- **Q6**: `git diff --stat` перед кожним коммітом (перевірка ізоляції prod).

## 🛑 Протокол STOP/CONTINUE (Критично)

- **STOP**: Миттєва зупинка автономних дій та інструментів.
- **Питання**: При наявності питання в стані STOP — надати відповідь і **зупинитися**.
- **CONTINUE**: Робота продовжується **ТІЛЬКИ** після явної команди («продовжуй», «ок», «виконуй»).
- **Заборона**: Будь-яка зміна коду/стану без підтвердження після STOP — критична помилка.

## 🔄 Захист від втрати контексту
- Обов'язкова перевірка `CONTEXT.md`, `task.md` та `Plan_01.md` на початку кожної сесії.
- Обов'язкове оновлення стану після кожної завершеної фази.
